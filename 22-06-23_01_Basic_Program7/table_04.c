#include<stdio.h>
void table(int no)
{
    for (int i = 0; i <no ; i++) {
        /* code here */ 
        printf("%d*%d=%d",no,i,no*i);
    }
}
void main()
{
    printf("Enter number:")
    table(no);
}
